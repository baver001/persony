/** 100% battery in integer energy units (config-driven in Phase 3). */
export const FULL_BATTERY_UNITS = 1_000_000;

/** Target AI gross margin — retail = provider / (1 - margin). */
export const TARGET_AI_GROSS_MARGIN = 0.8;

export const PRICING_VERSION = 'v0';
