# 12 — Professional tools & knowledge

**Статус:** foundation implemented

- `worker/tools/registry.ts` — ToolRegistry, permissions, cost stubs
- `knowledge_items` table — metadata; R2 binding `KNOWLEDGE_BUCKET` (optional)
